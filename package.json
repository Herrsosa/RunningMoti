{
  "name": "runningmoti-project",
  "version": "1.0.0",
  "private": true,
  "description": "RunningMoti main project",
  "scripts": {
    "vercel-build": "echo \"Using builds defined in vercel.json\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0"
  }
}
